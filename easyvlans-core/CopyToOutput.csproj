<Project>
  <Target Name="PostBuild" AfterTargets="PostBuildEvent">
    <Exec Command="xcopy /c /y &#34;$(TargetDir)\*&#34; &#34;$(SolutionDir)\_OUTPUT_$(Configuration)\&#34;"/>
  </Target>
</Project>
